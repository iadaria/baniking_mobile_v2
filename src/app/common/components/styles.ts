import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  padding: {
    paddingTop: 10,
    paddingRight: 40,
    paddingBottom: 18,
    /* borderWidth: 1,
    borderColor: 'red', */
  },
});
